kind = sequential
title = "汇编程序"
tests = 32
size = 128
copy_solution_to_architecture = "Overture"
unlocks_pages = ["Assembly/Language Creation", "Assembly/Assembly Programming"]
immutable_isa = true
dialogue = [
  (mentor_centered, "打孔式编程太无聊了。和上一关一样，这一关里你要计算一个整数加 5 的结果。不过这一次，你需要使用汇编语言。"),
  (mentor, `汇编语言本质上说，就是为电脑运行的指令取一个人类能读懂的别名。

习惯上，汇编语言的指令要遵循特定的语法：每条指令都以一个单词缩写开头（称作助记符，[color=#9966ff]mnemonic[/color]），用于描述指令的功能；其后跟随一组操作数（[color=#66ccff]operand[/color]），例如：
[highlight][color=#9966ff]mov[/color] [color=#66ccff]r1[/color], [color=#66ccff]r2[/color][/highlight]

另外，习惯上，指令的作用目标总是放在操作数列表的最左侧。比如，上面这条指令的意思是将寄存器 r2 中的值复制（移动）到 r1 中。理解这条规则有助于你阅读真实的汇编代码。

单击程序存储器右上角的“编辑”按钮可以打开汇编语言编辑器，你可以在其中使用汇编语言编程。此外，你可以单击编辑器左上角的“汇编模式”按钮，将编辑器切换到“指令集模式”。在指令集模式下，你可以查阅 Overture 架构所支持的所有汇编指令。`),
  (overture),
]
#7ef6dc35c7a7a3c6882cc456c589ead5