[settings]
name = "Overture"

[fields]

in_register
r0 000
r1 001
r2 010
r3 011
r4 100
r5 101
in 110

out_register
r0 000
r1 001
r2 010
r3 011
r4 100
r5 101
out 110

[instructions]

mov %a(out_register), %b(in_register)
10bbbaaa
# 将寄存器 %b 的值写入寄存器 %a。

imm %a(immediate | label)
00aaaaaa
# 将固定值 %a 写入寄存器 r0。

nand
01000000
# 对寄存器 r1 和 r2 的值执行按位与非运算，并将结果存入寄存器 r3。

or
01000001
# 对寄存器 r1 和 r2 的值执行按位或运算，并将结果存入寄存器 r3。

and
01000010
# 对寄存器 r1 和 r2 的值执行按位与运算，并将结果存入寄存器 r3。

nor
01000011
# 对寄存器 r1 和 r2 的值执行按位或非运算，并将结果存入寄存器 r3。

add
01000100
# 将寄存器 r1 和 r2 的值相加，并将结果存入寄存器 r3。

sub
01000101
# 将寄存器 r1 和 r2 的值相减，并将结果存入寄存器 r3。

nop
11000000
# 不执行任何操作。

jmp
11000001
# 无条件跳转至寄存器 r0 的值对应的地址。

jz
11000010
# 当寄存器 r3 的值等于 0 时，跳转至寄存器 r0 的值对应的地址。

jnz
11000011
# 当寄存器 r3 的值不等于 0 时，跳转至寄存器 r0 的值对应的地址。

jl
11000100
# 当寄存器 r3 的值小于 0 时，跳转至寄存器 r0 的值对应的地址。

jge
11000101
# 当寄存器 r3 的值大于等于 0 时，跳转至寄存器 r0 的值对应的地址。

jle
11000110
# 当寄存器 r3 的值小于等于 0 时，跳转至寄存器 r0 的值对应的地址。

jg
11000111
# 当寄存器 r3 的值大于 0 时，跳转至寄存器 r0 的值对应的地址。
#db2bc99369dd74264c3689d13de06fe8