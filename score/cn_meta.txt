title = "去芜存菁"
dialogue = [
  (mentor_centered, "在这一关里，我们将会引入评分系统。你的电路分数取决于使用的逻辑门数量、电路响应时间和每个时钟周期的能耗。")
  (mentor_centered, "下面两个算式的值相等，但它们并不等价：

((A + B) + C) + D
(A + B) + (C + D)

它们的结果相同，但后一个式子可以并行计算，因此它可以比第一个算式更早输出结果。"),
  (mentor_centered, `每个基本逻辑门（与非、或非、与、或）都有 1 单位的响应延迟时间。也就是说，它们会在接收到输入信号的 1 单位时间内完成计算。

一个电路中，从输入到输出端的不同路径有不同的延迟量。整个电路的响应延迟量取决于其中最慢的那条路径，即瓶颈路径（the critical path）。
你可以点击窗口右上角的沙漏图标，来查看瓶颈路径。`),
  (mentor_centered, "此外，每种基本逻辑门在 1 单位时间内还会产生 1 单位能耗。

所以电路在一个时钟周期（时钟刻）内的能耗，等于你所使用的基本逻辑门的总数乘以瓶颈路径的响应延迟量。"),
  (mentor_centered, "你在之前的关卡中解锁的元件成本取决于对应关卡的得分。

如果你希望降低已有元件的成本，你可以回头优化对应关卡的解法，以减少逻辑门的数量或延迟量。

要想通过这一关，你的异或门必须只包含不多于 3 个基本逻辑门，延迟量也不能超过 2。")
  (mentor_centered, "在本关里，你需要读取输入值，将其加 1 后输出。

结果中超出 8 位的部分直接舍去即可。

本关的额外要求是你的电路必须只包含至多 40 个基本逻辑门，延迟量不得高于 10。")
]
#0af2db8b8dba1839d491906e12bb3152