kind = sequential
no_score = true
title = "Функции"
immutable_isa = true
copy_solution_to_level = ["symphony_12_budget"]
copy_solution_to_architecture = "Symphony"
shared_data_folder = "symphony"
no_score = true
size = 255
unlocks_pages = ["Computer concepts/ASCII encoding"]
shared_data_folder = "symphony"
dialogue = [
  (mentor_centered, `Иногда полезно повторно использовать участок кода. Такие участки мы называем «функциями». Чтобы повторно использовать код, нужно перейти в начало функции и вернуться обратно в конце. Переход к функции называется «вызовом» функции, а возврат из функции — «возвратом».

Но чтобы этот участок кода действительно можно было повторно использовать, возврат должен уметь возвращаться в разные места в зависимости от того, откуда мы вызывали функцию.

Мы могли бы сохранить значение счётчика в регистр перед прыжком к функции и использовать это значение при возврате. Однако в таком случае функция не смогла бы вызвать другую функцию, потому что это перезаписало бы исходный адрес возврата.

Представьте, что функция A вызывает функцию B, а B вызывает функцию C. Когда мы хотим вернуться из C, нам нужен только адрес возврата для B, а в B — только адрес для A. В общем случае, независимо от того, как функции вызывают друг друга, последний добавленный адрес возврата всегда будет первым, который нам снова понадобится. Именно так работает стек.
`),
(mentor_centered, `На этом уровне в список инструкций добавлены «call» и «ret». Обе они — сложные синонимы.

«call» используется для входа в функцию. Он помещает адрес следующей инструкции в стек (читая счётчик и прибавляя смещение). Затем выполняет прыжок в начало функции.

«ret» используется для возврата из функции. Он извлекает адрес возврата из стека и выполняет прыжок по нему.`),
(mentor_centered, `Поскольку функции, скорее всего, изменяют значения регистров, мы можем захотеть поместить значения в стек для сохранения, а потом извлечь их обратно для восстановления. Неважно, делается это внутри вызванной функции или снаружи, но хорошей практикой считается установить соглашение (так называемое «calling convention»), чтобы все функции были совместимы друг с другом.`),
  (mentor_centered, `Этот уровень только для того, чтобы вы увидели, как работают функции. Здесь нет тестов. Обязательно прочитайте ассемблерный код и пройдитесь по нему до конца, чтобы понять, как всё работает. Это понадобится в следующих уровнях.`),
]
#2cb12258562000fc6da9571eff9fc402