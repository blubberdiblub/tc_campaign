kind = sequential
immutable_isa = true
title = "Superscalar"
max_delay = 75
shared_data_folder = "symphony_allegro"
copy_solution_to_architecture = "Symphony_super"
copy_solution_to_level = ["symphony_14_speculative_execution"]
size = 255
dialogue = [
  (mentor, `Time to add another pipeline and go superscalar! 

As I mentioned before, superscalar means we are executing more than one instruction per tick, in this case two.`),
  (info, `These are the things you need to do to add the second pipeline:

1. The instruction port is now 64 bit, split the output into two instructions
2. Add the data hazard component. When it is [ON] (and the JUMP timer is [OFF]) we can execute the second instruction
3. Place the updated counter
4. Add the new load and store ports to the register file (put the store ports at the bottom so they execute last)
5. Add a second ALU and wire everything together for the second pipeline
`)]

