title = "如此包装"
dialogue = [
    (mentor_left, `和之前的关卡一样，这一关里，我们会引入一些新的指令，而你需要实现这些指令对应的操作。不一样的地方在于，这一关里，你不需要调整你的硬件和电路布局。你需要通过修改汇编指令集架构（instruction set architecture，缩写为 ISA），利用已有的硬件功能来实现新的指令。

请打开汇编代码编辑器，点击窗口最左上方的“汇编模式”（ASM）按钮，将编辑器切换到指令集模式（ISA）。你需要先阅读已有指令的定义，了解每条指令是如何实现的，然后向指令集中添加下面三条指令：

[box]mov %a(register), %b(immediate)
将立即数 %b 存入寄存器 %a。

neg %a(register), %b(register)
对寄存器 %b 的值求相反数，将结果存入寄存器 %a。

not %a(register), %b(register)
对寄存器 %b 的值按位取反，将结果存入寄存器 %a。[/box]

定义好这三条指令后，你就可以运行汇编代码来通过这个关卡了。
`),
]
#675f216f8bbcb8916ed8d341d5fde564