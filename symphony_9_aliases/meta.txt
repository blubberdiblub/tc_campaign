kind = sequential
title = "Instruction aliases"
size = 255
no_score = true
copy_solution_to_level = ["symphony_10_stack"]
shared_data_folder = "symphony"
dialogue = [
    (info, `This level like previous levels, introduces new instructions. However, in this level you are not supposed to add additional hardware, but instead change the assembly specification to reuse already existing instruction hardware for new instructions.

Click on the "ISA" button in the top left corner of the Assembler window and study how instructions are implemented. Then add the following five new instructions:

mov %a(register), %b(immediate)
Copies the immediate value %b to register %a.

neg %a(register), %b(register)
Negate register %b and store the result in %a.

not %a(register), %b(register)
Bitwise NOT each bit in register %b and store the result in %a.

movt %a(register), %b(register)
Moves lower 16 bits of %b to the top 16 bits of %a, setting the low 16 bit to zero.

movb %a(register), %b(immediate)
Moves the immediate value %b to the lower 2 bytes of %a, preserving the top 16 bits.

Once these instructions are added, the assembly code will be valid and running it will complete the level.
`),
]


