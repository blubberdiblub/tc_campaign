title = "Псевдонимы инструкций"
dialogue = [
    (mentor_left, `Как и в предыдущих уровнях, здесь вводятся новые инструкции. Однако на этом уровне не нужно добавлять дополнительное железо — вместо этого измените спецификацию ассемблера, чтобы уже существующие аппаратные блоки могли выполнять новые инструкции.

Нажмите кнопку «ISA» в левом верхнем углу окна Ассемблера и изучите, как реализованы инструкции. Затем добавьте следующие три новые инструкции:

[box]mov %a(register), %b(immediate)
Копирует непосредственное значение %b в регистр %a.

neg %a(register), %b(register)
Отрицает регистр %b и сохраняет результат в %a.

not %a(register), %b(register)
Побитово инвертирует каждый бит регистра %b и сохраняет результат в %a.[/box]

После добавления этих инструкций ассемблерный код станет корректным, и его выполнение пройдёт уровень.
`),
]
#675f216f8bbcb8916ed8d341d5fde564