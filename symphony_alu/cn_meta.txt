kind = combinational
hint_kind = combinational
title = "更新换代"
size = 48
copy_solution_to_foundry = "Symphony/ALU"
copy_solution_to_level = ["symphony_fast_alu"]
background = 6
dialogue = [
  (mentor_centered, "我们给你 Overture 架构的 ALU 制作了一个副本。现在是时候将它也升级到 32 位，并为其添加一些新的计算功能了。

需要实现的操作码列表：

[color=#87a8c8]0 NAND（按位与非）
1 OR（按位或）
2 AND（按位与）
3 NOR（按位或非）
4 ADD（加法）
5 SUB（减法）
6 XOR（按位异或）
7 LSL（逻辑左移）
8 LSR（逻辑右移）
9 ASR（算术右移）
10 CMP（比较大小）[/color]

其中计算比较标志的元件 cmp 正是你在“举一反三”这一关中搭建好的那个版本。"),
]
#07dd22adbbb3a3e08160c9728993ff46