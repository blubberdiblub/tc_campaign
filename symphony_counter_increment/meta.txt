kind = sequential
title = "Counter increment"
size = 48
background = 6
max_delay = 64
copy_solution_to_foundry = "Symphony/Allegro/cnt_inc"
dialogue = [
    (mentor, `When we execute two instructions, our counter needs to increment by 8. However, if we are not executing the second instruction, we should only increment by 4. And finally, in certain situations neither instruction execute and we shouldn't increment at all.

Build a circuit that can take an address and increment an appropriate amount based on how many instructions execute.`)
]
