kind = sequential
title = "Cache controller"
size = 255
max_delay = 64
copy_solution_to_foundry = "Symphony/Allegro/cache_ctrl"
background = 6
dialogue = [
  (info, `

TODO, make sure we check RAM is 0xffff bytes

Explain config delay and the latency ram itself

  `),
]
